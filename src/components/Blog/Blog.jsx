import PropTypes from 'prop-types';

const Blog = (props) => {
console.log(props);
    return (
        <div>
            
        </div>
    );
};

Blog.propTypes = {
    Blog: PropTypes.object.isRequired,
}

export default Blog;